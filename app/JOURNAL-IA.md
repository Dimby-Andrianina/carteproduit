# Journal IA — Mini-TP 4 — Rambeloson Raitra Fiorenana

- Variante soumise : mise en page de `ProduitCard` en `Row` (infos produit à gauche, quantité + bouton à droite) au lieu d’une `Column` unique.
- Mon jugement (3 lignes) :
  Je garde ma version en `Column` : le code reste plus simple, linéaire et fidèle au modèle du cours, sans `Arrangement` ni imbrication supplémentaire.
  La variante en `Row` est lisible et peut mieux utiliser l’espace en paysage, mais elle alourdit la structure pour une carte encore peu chargée.
  Je nuance donc : l’idée d’alignement horizontal est intéressante plus tard ; pour ce mini-TP, la `Column` d’origine est plus cohérente et plus facile à maintenir.